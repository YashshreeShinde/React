import React from 'react'
import styles from "./Header.module.scss"

const Header = () => {
  return (
    <div className={styles["header"]}>
        <span>Diwali Leaves</span>
    </div>
  )
}

export default Header